// src/features/roadmap/constants.ts
import type { DifficultyLevel } from '@/shared/types';

export const difficultyOptions: DifficultyLevel[] = [
  'All',
  'Easy',
  'Medium',
  'Hard',
];

// You can add other roadmap-specific constants here if needed
